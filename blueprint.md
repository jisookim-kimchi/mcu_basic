 우선순위
1순위: I2C 드라이버 개발 (센서의 눈)
목표: 밸런싱 로봇의 기울기(각도)를 측정하기 위해 MPU6050 자이로/가속도 센서 값을 읽어와야 합니다.
해야 할 일: I2C 통신(Polling 방식 먼저 개발 ➡️ 이후 Interrupt/DMA 확장) 드라이버를 만듭니다.
이유: 각도를 읽지 못하면 로봇은 균형 제어(PID)를 아예 시작할 수 없습니다.

2순위: Timer 드라이버 개발 (모터 발과 속도 측정)
목표: 모터의 속도를 제어(PWM)하고, 바퀴의 회전 속도를 측정(Encoder)해야 합니다.
해야 할 일:
PWM 출력 모드: 타이머를 설정하여 모터 드라이버에 보낼 PWM 신호 생성 기능을 만듭니다.
Encoder 모드: STM32 타이머 내부의 하드웨어 엔코더 모드(Timer Encoder Mode)를 활성화하여 바퀴 회전수를 하드웨어적으로 카운팅합니다. (GPIO 인터럽트보다 훨씬 정확하고 안정적입니다.)
이유: 모터를 굴리고 바퀴 속도를 피드백 받기 위해 필수적입니다.

3순위: UART 드라이버 개발 (튜닝 및 조종)
목표: 로봇의 균형을 잡기 위해 PID 제어 파라미터($K_p$, $K_i$, $K_d$)를 실시간으로 튜닝하고 원격 조종을 해야 합니다.
해야 할 일: UART 송수신 드라이버를 만들고 PC나 블루투스 모듈과 연동합니다.
이유: 밸런싱 로봇 제작 시간의 80%는 넘어지지 않게 값을 미세 조정(Tuning)하는 데 쓰입니다. UART가 없으면 로봇이 왜 넘어지는지 각도 그래프를 볼 수 없어 튜닝이 불가능에 가깝습니다.

4순위: EXTI 드라이버 연동 (센서 정밀화)
목표: 자이로 센서가 데이터를 준비 완료했을 때(INT 핀 출력) 즉시 인터럽트로 읽어오는 기능을 구현합니다.
해야 할 일: 이미 구현해 두신 EXTI 드라이버와 GPIO 드라이버를 묶어 작동시킵니다.
💡 추천하는 다음 액션
현재 계획(🚧 Planned) 목록에 I2C가 첫 타겟으로 지정되어 있습니다.

로봇 제어의 첫걸음인 기울기 센서 값 읽기를 위해 I2C 드라이버 개발부터 시작하시는 것을 강력히 추천합니다!

